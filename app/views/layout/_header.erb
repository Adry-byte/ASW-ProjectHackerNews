<div class="header">
  <div class="links"></div>

  <a href="/submissions/">  <img src="<%= image_path('asw.png') %>
" alt="logo" />
     <h1>ASW NEWS</h1></a>
  <div class="links">
    <a href="/submissions/">new</a></span>
      |
    <a href="/comments/">comments</a></span>
    |
    <% if logged_in? %>
        <a href="/threads/">threads</a></span>
        |
      <%= link_to "ask", ask_path %>
          |
      <a href="/submissions/new">submit</a></span>
    <% else %>
      <%= link_to "ask", ask_path %>
          |
      <a href="/submissions/new">submit</a></span>
    <% end %>
    <div class="right">
    <% if logged_in? %>
          <a href="/users/<%= current_user().id %>/edit"><%= current_user().username %></a>
        |
        <%= link_to "log out", logout_path, method: "delete" %>
    <% else %>
        <%= link_to "log in", login_path %>
    <% end %></div>
  </div>
</div>